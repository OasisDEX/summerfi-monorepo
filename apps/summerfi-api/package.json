{
  "name": "lambdas",
  "version": "1.0.0",
  "scripts": {
    "tsc": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip-assets": "zip -r artifacts/portfolio-assets.zip dist/shared dist/portfolio-assets",
    "zip-overview": "zip -r artifacts/portfolio-overview.zip dist/shared dist/portfolio-overview",
    "build": "pnpm run tsc && mkdir -p artifacts && pnpm run zip-assets && pnpm run zip-overview"
  },
  "author": "Piter",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.76",
    "@types/node": "^20.9.0"
  },
  "dependencies": {
    "graphql": "^16.8.1",
    "graphql-request": "^6.1.0",
    "typescript": "^5.2.2"
  }
}
