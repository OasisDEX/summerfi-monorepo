{
  "name": "@summerfi/portfolio-overview-function",
  "version": "1.0.0",
  "scripts": {
    "tsc": "tsc",
    "watch": "tsc -w",
    "test": "jest --passWithNoTests",
    "build": "tsc -b -v",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "docker:build": "tar chzf files.tar.gz Dockerfile node_modules dist && docker build --platform linux/amd64 -t portfolio-overview - < files.tar.gz && rm files.tar.gz",
    "docker:run": "docker run --rm -p 9000:8080 portfolio-overview",
    "dev": "npm run tsc && npm run docker:build && npm run docker:run",
    "invoke": "curl http://localhost:9000/2015-03-31/functions/function/invocations -d '{\"queryStringParameters\": {\"address\": \"0x10649c79428d718621821Cf6299e91920284743F\"}}' | jq",
    "aws:deploy": "deploy zip file"
  },
  "devDependencies": {
    "@summerfi/eslint-config": "workspace:*",
    "@summerfi/typescript-config": "workspace:*"
  },
  "dependencies": {
    "graphql": "^16.8.1",
    "graphql-request": "^6.1.0",
    "@summerfi/serverless-shared": "workspace:*",
    "zod": "^3.22.4",
    "node-fetch": "^3.3.2"
  }
}
