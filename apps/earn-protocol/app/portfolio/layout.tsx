import { OffsetLayout } from '@/components/layout/OffsetLayout/OffsetLayout'

export default function PortfolioLayout({ children }: { children: React.ReactNode }) {
  return <OffsetLayout>{children}</OffsetLayout>
}
