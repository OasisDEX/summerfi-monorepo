version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npx pnpm install
    build:
      commands:
        - npx pnpm build
  artifacts:
    baseDirectory: apps/earn-protocol/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - /**/*/node_modules/**/*
