# Function: isPool()

```ts
function isPool(maybePool): maybePool is IPool;
```

Defined in: [../sdk-common/src/common/interfaces/IPool.ts:45](https://github.com/OasisDEX/summerfi-monorepo/blob/dev/src/common/interfaces/IPool.ts#L45)

Type guard for IPool

## Parameters

### maybePool

`unknown`

## Returns

`maybePool is IPool`

true if the object is an IPool
