import type { PublicClient } from 'viem'

/**
 * @name BlockchainClient
 * @description A configuration item that can be retrieved from the configuration provider
 */
export type BlockchainClient = PublicClient
