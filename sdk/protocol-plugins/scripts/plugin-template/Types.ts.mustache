import { {{name}}ContractNames } from '@summerfi/deployment-types'
import { CollateralConfig, DebtConfig } from '@summerfi/sdk-common/protocols'
import { AddressValue } from '@summerfi/sdk-common/common'
import type { LendingPool } from '@summerfi/sdk-common/protocols'
import { ERC20_ABI } from './abis'

// Lending Pool
export interface {{name}}PoolDebtConfig extends DebtConfig {}
export interface {{name}}PoolCollateralConfig extends CollateralConfig {
}

export type {{name}}LendingPool = LendingPool<{{name}}PoolCollateralConfig, {{name}}PoolDebtConfig>

// ABIs and Protocol contracts
export type {{name}}AbiMap = {
 // Dog: typeof DOG_ABI
}

export type {{name}}AddressAbiMap = {
  [K in {{name}}ContractNames]: {
    address: AddressValue
    abi: {{name}}AbiMap[K]
  }
}
