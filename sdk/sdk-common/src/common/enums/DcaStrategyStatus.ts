/** Lifecycle status of a DCA (dollar-cost-averaging) strategy. */
export enum DcaStrategyStatusEnum {
  Active = 'ACTIVE',
  Paused = 'PAUSED',
  Cancelled = 'CANCELLED',
  Completed = 'COMPLETED',
}
