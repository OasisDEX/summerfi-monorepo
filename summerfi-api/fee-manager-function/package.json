{
  "name": "@summerfi/fee-manager-function",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node20 --outfile=dist/index.js --sourcemap",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "generate": "DOTENV_CONFIG_PATH=../../.env graphql-codegen -r dotenv/config --config codegen_aave_like.yml && DOTENV_CONFIG_PATH=../../.env graphql-codegen -r dotenv/config --config codegen_ajna_v2.yml"
  },
  "devDependencies": {
    "@summerfi/eslint-config": "workspace:*",
    "@summerfi/typescript-config": "workspace:*",
    "@types/node": "^20.12.7",
    "eslint": "^8.57.0"
  },
  "dependencies": {
    "@oasisdex/dma-library": "0.6.65",
    "@summerfi/serverless-shared": "workspace:*",
    "graphql-request": "^6.1.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  }
}
