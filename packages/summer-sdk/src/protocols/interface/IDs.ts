/**
 * @interface IProtocolId
 * @description Represents a protocol's ID. This will be specialized for each protocol
 */
export interface IProtocolId {
    id: string;
}

/**
 * @interface IPoolId
 * @description Represents a pool's ID. This will be specialized for each protocol
 */
export interface IPoolId {
    id: string;
}
