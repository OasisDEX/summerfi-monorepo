import { IPercentage } from "./IPercentage";

/**
 * @interface Risk ratio
 * @description Risk ratio representing the risk of position, typically the LTV
 */
export interface IRiskRatio {
    ratio: IPercentage;
}
