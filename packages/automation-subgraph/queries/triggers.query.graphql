query Triggers($dpm: Bytes!) {
  triggers(where: { account: $dpm, removedBlock: null }) {
    account
    owner
    id
    protocol
    triggerData
    triggerType
    addedTimestamp
    decodedData
    decodedDataNames
  }
}
