query Triggers($dpm: Bytes!) {
  triggers(where: { account: $dpm, executedBlock: null, removedBlock: null }) {
    account
    owner
    id
    protocol
    triggerData
    triggerType
    addedTimestamp
    decodedData
    decodedDataNames
  }
}
