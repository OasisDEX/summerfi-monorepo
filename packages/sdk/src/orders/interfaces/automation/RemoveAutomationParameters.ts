import { AutomationId } from './AutomationId'

/**
 * @interface RemoveAutomationParameters
 * @description Parameters used to add automation to a position
 */
export interface RemoveAutomationParameters {
  automationId: AutomationId
}
