import { Simulation } from '@summerfi/sdk'
import { StrategyDefinition } from './Strategy'

export class OrderPlanner {
  /* eslint-disable-next-line @typescript-eslint/no-unused-vars */
  public buildOrder(simulation: Simulation, strategy: StrategyDefinition): void {
    return undefined
  }
}
